---
tags:
  - AI/agents
  - context
created: 2026-07-07
---

# Agent Context 管理学习指南（Compaction 篇）

> [!info] 定位与来源
> 综述。梳理 agent harness 的 context window 管理与压缩（compaction）机制，
> 以 Claude Code、Pi、Manus 等生产实现为佐证案例。
> 与 [[Agent-Memory-学习指南]] 互补：记忆篇管跨 session 的长期信息传递，
> 本文管单个长任务内 context window 的存续——同属"长程技术"，分工不同。
>
> 全文按「通用层 → 三级压缩阶梯 → 稳定性 → 横向对比」组织：
> §1–§2 讲问题定位、公理与 When/What/How 三问模型；
> §3–§4 讲两个通用问题（何时压、压什么）；
> §5–§7 按信息损失从零到不可逆展开三级手段；
> §8–§10 稳定性、横向对比与设计 checklist。

![[context-compaction-map.png]]（全局图占位，骨架 = 三问模型 + 三级阶梯）

## 1 压缩解决什么问题

- context 的三层时间尺度，先定位再谈手段：
  - 短期 = active context window（当前正在推理的工作记忆）；
  - 中期 = session state 与跨 run 持久化；
  - 长程 = 跨 session 技术，含 compaction 与记忆系统两条线
    （记忆见 [[Agent-Memory-学习指南]]，本文只讲 compaction）。
- 边界划清：压缩 vs 记忆（压缩为当前任务的继续服务，产物留在 context；
  记忆为未来 session 服务，产物进外部存储）；压缩 vs 简单截断。
- 三条公理：
  - **公理一：有损摘要的误差会级联。** 原文一旦丢弃，摘要就成为后续所有轮次
    的唯一事实来源，错了无从核对。推论：**手段按信息损失排序**——
    无损 → 可逆 → 有损，前一级不够用才动用下一级（§5–§7 的阶梯即由此而来）。
  - **公理二：token 用量只能估算，估算会漏判。** 推论：**主动触发之外必须有
    被动兜底**——阈值留余量 + 撞墙被动压双保险（见 §3「When：什么时候压」）。
  - **公理三：压缩本身耗时，交互主链路等不起。** LLM summary 是一次完整模型
    调用。推论：**提前异步压是主路径**，同步压只做补救
    （与记忆篇公理三同源：重活移出主链路）。

## 2 统一模型与决策结构

- 任何 compaction 方案都拆成三问：**When（什么时候压）× What（压什么）×
  How（怎么压）**。
- 三问的决策性质不同，对应不同的出错方式（呼应记忆篇 §2 的两类划分）：
  - When 是**通用轴**：执行时机全集只有三种（提前异步 / 临时同步 / 撞墙被动），
    可叠加、非互斥，各家差别只在开了哪几个、阈值填多少——错误形式是填错参数；
  - What 是**分流器**：按内容可否规则识别分成两路，分类结果决定进 How 的哪条
    路径——错误形式是分错路（把结构化内容交给 LLM summary 是浪费，反之是丢信息）；
  - How 是**三级阶梯**：与记忆篇三范式"互斥三选一"恰好相反——三级不是挑一个，
    而是**按损失递增依次叠加**，便宜的先做、昂贵的兜底。
- 全文结构表（部分 / 内容 / 章节）。

## 3 When：什么时候压

- **判断依据**：实际 token 用量，不是轮数、不是字符数估算。
- **三种执行时机**（按主动性递减，可叠加）：
  - 提前异步压——主路径，在后台先压好，主链路无感（公理三的落地）；
  - 异步没压完就临时同步压（Claude Code）——异步产物没赶上时的降级；
  - 撞墙被动压（Claude Code、Pi）——请求真的超限后被动触发；
    存在的理由是 token 估算漏判（公理二），主动策略防不住的都由它兜底。
- **硬约束**：绝不切在 tool result 上——压缩边界必须保持 tool_use /
  tool_result 配对完整，切断配对会直接产生非法请求。

## 4 What：压什么

- 内容按"能否被规则识别"分两类，分类结果决定 How 的路径：
  - **结构化内容——纯规则识别**：tool result、成对消息、可枚举的消息类型，
    程序直接处理，不调 LLM，零判断风险 → 走 §5「无损清理」与 §6「可逆折叠」；
  - **非结构化内容——LLM summary**：对话叙事、推理过程、决策脉络，
    只有 LLM 能判断轻重 → 走 §7「有损摘要」。
- 这条分流是三级阶梯成立的前提：先用规则把结构化部分处理干净，
  留给有损摘要的体量越小，公理一的风险敞口越小。

## 5 How 第一级：无损清理

- 特征：被清理的信息或可从别处恢复、或本就无残余价值，零信息损失。
- 手段：
  - 清理碎片；
  - 小 tool result 占位替换；
  - 删配对（tool_use / tool_result 成对移除）；
  - microcompact（Claude Code）——上述规则清理的工程化形态。

## 6 How 第二级：可逆／折叠

- 特征：原文仍在（文件系统 / 外部存储），context 里只留指针或短摘要，
  需要时可取回——与记忆篇 §5.2 的 progressive disclosure 同构：
  常驻层只留导航信息，深层内容按需加载。
- 手段：
  - 大内容换为链接 / 短摘要（Manus，文件系统当外存）；
  - context collapse（Claude Code）；
  - tool result 截断到 2000 字符（Pi）。

## 7 How 第三级：有损摘要

- 特征：不可恢复，公理一直接管辖——本级的全部设计都在控制"丢什么"。
- **重要性分级**（LLM summary 的取舍标准）：
  - high：用户指令、纠正与偏好变化、用户反馈、具体数据值；
  - medium：影响决策的工具结果、中间推理、背景上下文、澄清交流；
  - low：寒暄、冗余确认、冗长工具输出、重复信息。
  - （对照记忆篇 §3 的写入门控：high 名单与记忆的"该记什么"高度重合，
    同一条价值判断在两条线上各自出现。）
- CoT 丢弃（Claude Code）：中间推理属可丢内容。
- **结构化段落 / 固定摘要框架**（Claude Code、Pi）：给 summary 一个固定
  schema，逼模型按栏目填空而不是自由发挥——输出结构稳定是下一节抗漂移的基础。

## 8 稳定性：怎么不崩

- **抗摘要漂移**：多次压缩后"摘要的摘要"逐渐失真，是长任务的主要崩坏模式
  ——公理一的多轮累积形态。
- 对策：**保留边界，不重复写老内容**（Claude Code、Pi）——每轮压缩只处理
  新增量，已压缩的旧摘要保持原样，不再次进入摘要循环，误差不叠代。
- 衔接记忆篇：压缩前先抽取记忆（记忆篇 §4.1 的 OpenClaw 变体），
  防止信息在压缩中永久丢失——两条线在这个时机上交汇。

## 9 横向对比与方案地图

- 对比表：触发时机组合 / 无损手段 / 折叠手段 / 有损框架 / 抗漂移设计
  × Claude Code / Pi / Manus。
- 逐方案评价（整机视角：优势、局限、什么场景参考它）。
- 收敛趋势：规则清理前置、summary 框架化、异步化。

## 10 设计 checklist

**When**
- [ ] 触发以实际 token 用量为准，阈值留安全余量
- [ ] 提前异步压为主路径，同步压与撞墙被动压双兜底
- [ ] 压缩边界不切断 tool_use / tool_result 配对

**What / How**
- [ ] 结构化内容全部走规则处理，不进 LLM summary
- [ ] 三级阶梯顺序执行：无损 → 可逆 → 有损，前级不够才进下级
- [ ] 大内容外置留指针，保留取回路径
- [ ] 有损摘要带重要性分级与固定框架

**稳定性**
- [ ] 旧摘要不重摘，只压新增量
- [ ] 压缩前触发记忆抽取（衔接记忆管线）
- [ ] 评测闭环：压缩后任务成功率如何度量

## 参考文献

（占位：Claude Code / Pi / Manus 的一手资料，主参考文章链接）

相关笔记：[[Agent-Memory-学习指南]]
